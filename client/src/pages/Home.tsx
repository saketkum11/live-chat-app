import "@fontsource/roboto/vietnamese-400.css";
import { Box } from "@mui/material";
const Home = () => {
  return (
    <div>
      <Box component="section" sx={{ border: "2px solid grey" }}>
        This Box renders as an HTML section element.
      </Box>
    </div>
  );
};

export default Home;
